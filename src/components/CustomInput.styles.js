import { makeStyles } from "tss-react/mui";

export const styles = (theme) => ({
  paper: {
    padding: "2px 4px",
    display: "flex",
    alignItems: "center",
  },
});

export default makeStyles()(styles);
